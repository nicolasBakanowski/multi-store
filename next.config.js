/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost"], // Agrega el dominio "localhost" aquí
  },
};

module.exports = nextConfig;
